import React from 'react';

export default class extends React.Component {
  constructor(props) {
    super(props);
  }

  static displayName = 'SnackBar'
  static propTypes = {}
  render() {
    return (
      <div></div>
    );
  }
}
